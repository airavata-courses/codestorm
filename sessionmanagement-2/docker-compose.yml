version: "2"
networks:
    default:
        external:
            name: codestorm-default-network

services:
    sessionmanagement-2:
        container_name: sessionmanagement-2
        restart: always
        build:
            context: .
            dockerfile: Dockerfile
        image: sessionmanagement-2:1.0
        ports:
            - "8089:8089"
